curve CartesianGrid(n):
	(CartesianX(n),CartesianY(n))

curve CartesianX(n):
	part:
		GridAux1(n)
	part:
		GridAux2(n)

curve CartesianY(n):
	part:
		GridAux2(n)
	part:
		GridAux1(n)

curve GridAux1(n):
	floor(t*(n+1))/n

curve GridAux2(n):
	mod(t*(n+1),1)