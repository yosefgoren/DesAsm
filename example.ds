curve Circle:
	(sin(2*pi*t), cos(2*pi*t))

curve connect(v, u):
	v*t + u*(1-t)

curve Polygon(n):
	for i in n:
		connect(t, Circle(i/n), Circle(mod(i+1,n)/n))

curve Triangle():
	Polygon(t,3)

curve Multi:
	for i in 3:
		for j in 5:
			for k in 6:
				Triangle(t)*k*0.25 + (2*i, 2*j)
	~ (t,t)
