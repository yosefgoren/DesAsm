curve Circle:
	(sin(2*pi*t), cos(2*pi*t))

curve connect(v, u):
	v*t + u*(1-t)

curve Triangle():
	for i in 3:
		connect(t, Circle(i/3), Circle(mod(i+1,3)/3))

func squared(n):
	n^2

curve Multi:
	for i in 3:
		for j in 5:
			for k in 6:
				Triangle(t)*squared(k)*0.25 + (2*i, 2*j)
