curve Circle:
	(sin(2*pi*t), cos(2*pi*t))

curve connect(v, u):
	v*t + u*(1-t)

curve Polygon(n):
	for i in n:
		connect(t, Circle(i/n), Circle(mod(i+1,n)/n))

curve Multi:
	for i in 4:
		for j in 4:
			Polygon(t, 4*i+j)+(2*j-5, 2*i-5)
	~
	Polygon(t,20)*2*pi

